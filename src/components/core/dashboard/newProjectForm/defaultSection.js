const colors = [
    "#47cc8a",
    "#00aaff",
    "#30bfbf",
    "#8acc47",
    "#8f7ee6",
    "#ffd500",
    "#ff9f1a",
    "#d93651",
    "#98aab3"
];

const getRandomNumber = (min, max) => {
    return Math.floor(Math.random() * (max - min)) + min;
};

export { colors, getRandomNumber };

// #edf0f1
// #f0f0f4
